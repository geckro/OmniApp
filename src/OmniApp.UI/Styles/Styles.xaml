<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:gameWindows="clr-namespace:GameManager.UI.Windows;assembly=GameManager.UI">

  <Style x:Key="BaseGameWindowStyle">
    <Style.Resources>
      <Style TargetType="Control" x:Key="BaseControlStyle">
        <Setter Property="Margin" Value="5" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontFamily" Value="Inter" />
        <Setter Property="Background" Value="{DynamicResource BlueSolid}" />
        <Style.Resources>
          <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="10" />
          </Style>
        </Style.Resources>
      </Style>
      <Style TargetType="DataGridColumnHeader">
        <Setter Property="Padding" Value="5, 2, 5, 2" />
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="0, 0, 1, 0" />
      </Style>
      <Style TargetType="DataGridCell">
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="0, 0, 1, 0" />
      </Style>
      <Style TargetType="{x:Type Button}" BasedOn="{StaticResource BaseControlStyle}">
        <Setter Property="BorderThickness" Value="0" />
        <Style.Triggers>
          <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Effect">
              <Setter.Value>
                <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="1" Direction="270" />
              </Setter.Value>
            </Setter>
          </Trigger>
        </Style.Triggers>
      </Style>
      <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource BaseControlStyle}">
        <Setter Property="BorderThickness" Value="0" />
      </Style>
      <Style TargetType="{x:Type Label}">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontFamily" Value="Inter" />
        <Setter Property="FontWeight" Value="Medium" />
      </Style>
      <Style TargetType="{x:Type ListBox}" BasedOn="{StaticResource BaseControlStyle}">
        <Setter Property="BorderThickness" Value="0" />
      </Style>
      <Style TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource BaseControlStyle}">
        <Setter Property="Padding" Value="2" />
      </Style>
      <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource BaseControlStyle}">
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="FontSize" Value="14" />
      </Style>
      <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource BaseControlStyle}" />
      <Style TargetType="{x:Type Menu}" BasedOn="{StaticResource BaseControlStyle}" />
    </Style.Resources>
  </Style>
  <Style TargetType="{x:Type gameWindows:MainWindow}" BasedOn="{StaticResource BaseGameWindowStyle}">
    <Setter Property="Background" Value="{DynamicResource BlueBase}" />
  </Style>
  <Style TargetType="{x:Type gameWindows:Add}" BasedOn="{StaticResource BaseGameWindowStyle}">
    <Setter Property="Background" Value="{DynamicResource BlueBase}" />
  </Style>
</ResourceDictionary>